import { SituationPipe } from './situation-pipe.pipe';

describe('SituationPipe', () => {
  it('create an instance', () => {
    const pipe = new SituationPipe();
    expect(pipe).toBeTruthy();
  });
});
